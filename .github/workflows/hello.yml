name: actions workflow
on: workflow_dispatch
jobs:
  action:
    runs-on: ubuntu-latest
    steps:
      - name: checkout 
        uses: actions/checkout@v4
      - name: print the file
        run: cat index.html
    
      
     
      
