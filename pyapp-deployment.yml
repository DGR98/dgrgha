apiVersion: apps/v1
kind: Deployment
metadata:
  name: pyapp
  namespace: pyapp
  labels:
    app: pyapp
spec:
  replicas: 4
  selector:
    matchLabels:
      app: pyapp
  template:
    metadata:
      labels:
        app: pyapp
    spec:
      containers:
        - name: pyapp
          image: gautham1999/gha-pyapp:995dbb5
          ports:
            - containerPort: 8000
