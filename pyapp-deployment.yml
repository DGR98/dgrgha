apiVersion: apps/v1
kind: Deployment
metadata:
  name: pyapp
  namespace: pyapp
  labels:
    app: pyapp
spec:
  replicas: 4
  selector:
    matchLabels:
      app: pyapp
  template:
    metadata:
      labels:
        app: pyapp
    spec:
      containers:
        - name: pyapp
          image: kammana/gha-pyapp:6818643
          ports:
            - containerPort: 8000
